# ============================================
# PixelVault - Environment Configuration
# ============================================
# Copy this file to .env.local and update with your credentials
# Command: cp env.example .env.local
# 
# IMPORTANT: This file contains placeholders. Replace all "your_*" values
# with your actual credentials. Never commit .env.local to git.

# Database Configuration (REQUIRED)
# Live Database Configuration
DATABASE_HOST=34.121.114.117
DATABASE_PORT=5432
DATABASE_NAME=images
DATABASE_USER=postgres
DATABASE_PASSWORD=your_database_password

# SSL Configuration (required for remote database)
DATABASE_SSL=true

# Optional Settings (you can leave these as default)
DATABASE_CONNECTION_TIMEOUT=10000
DATABASE_STATEMENT_TIMEOUT=120000
NODE_ENV=development
NEXT_PUBLIC_API_URL=http://localhost:3000

# Site URL for SEO (REQUIRED for production)
# Set this to your actual domain for production: https://yourdomain.com
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Google OAuth 2.0 Configuration (REQUIRED for Google Sign-In)
# Get these from: https://console.cloud.google.com/apis/credentials
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your_google_client_id
NEXT_PUBLIC_GOOGLE_REDIRECT_URI=http://localhost:3000/api/auth/google/callback
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
GOOGLE_REDIRECT_URI=http://localhost:3000/api/auth/google/callback